Implemented milestone #27 — Gentle default + parent gate + wordless Play shell. Contraption-lab was not committed or pushed.

Parent-setting model/storage: `src/state/parentSettings.ts` persists `{ timerFailureMode, puzzleLocking }` under `contraption-lab.parent-settings.v1`; both default `false`. Settings extends the existing panel with parent-only Timers and timeout and Puzzle locking controls. Existing `unlockAll` data/control remains available only while locking is enabled.

Default behavior: timed puzzle limits are hidden and their ticks are suppressed, so zero cannot enter Timeout/Failed; all built-in puzzles are selectable while earned completion continues to be recorded. Reset/Rerun/puzzle switching stay deterministic. Enabling timer/failure restores the existing countdown and Timeout/Failed behavior. Enabling locking restores sequential selection restrictions from the preserved progression data; disabling/re-enabling it does not delete earned completion.

Wordless Play shell: added visual affordances to Run/Pause, Edit, Rerun, Reset, Parts, and Next Puzzle; retained text and ARIA labels. Existing illustrated part previews remain the part identifiers; the puzzle control remains directly clickable; the Phaser Goal is now a star-marked cup and successful status receives a star affordance. No Storybook art redesign was added.

Files changed for #27: `docs/CODEX_PROMPTS.md`, `docs/PROJECT_STATE.md`, `docs/ROADMAP.md`, `e2e/puzzle.e2e.ts`, `index.html`, `src/game/PrototypeScene.ts`, `src/main.ts`, `src/state/gameState.ts`, `src/state/parentSettings.ts`, `src/state/progression.ts`, `src/style.css`, `src/ui/Controls.ts`, `tests/gameState.test.ts`, `tests/parentSettings.test.ts`, `tests/progression.test.ts`.

Tests/validation passed: `npm run typecheck`; `npm run lint`; `npm test` (13 files, 112 tests); `npm run format:check`; `npm run build` (only the existing Phaser chunk-size warning); `npx playwright test --list` (18 tests); `git diff --check`. Focused #27 browser command: `npx playwright test e2e/puzzle.e2e.ts --grep "uses gentle defaults and exposes parent options without text-dependent Play actions" --workers=1` — 1 passed (24.2s). Additional enabled-parent timer regression: `npx playwright test e2e/puzzle.e2e.ts --grep "times a puzzle through Run, Pause, Resume, Rerun, and Timeout" --workers=1` — 1 passed (18.3s).

Deliberately deferred: no #28 toppling Blocks, #29 art pipeline, Chapter-1 content, new mechanics, dependencies, accounts, or elaborate parental gate.

git status --short:
 M docs/CODEX_PROMPTS.md
 M docs/PROJECT_STATE.md
 M docs/ROADMAP.md
 M e2e/puzzle.e2e.ts
 M index.html
 M src/game/PrototypeScene.ts
 M src/main.ts
 M src/state/gameState.ts
 M src/state/progression.ts
 M src/style.css
 M src/ui/Controls.ts
 M tests/gameState.test.ts
 M tests/progression.test.ts
?? docs/reviews/
?? s-20260812-1003-c7d4-response.md
?? src/state/parentSettings.ts
?? tests/parentSettings.test.ts
