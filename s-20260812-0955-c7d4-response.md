Milestone #26 pre-commit fixes completed; contraption-lab was not committed or pushed.

Exact fix: validation now rejects legacy tag-only targets whenever `contacts` has the same tag twice (for example `['ball', 'ball']` with `target: 'ball'`). Same-tag rules must use `{ type: 'contact', index: 0 | 1 }` or `{ type: 'id', id }`. Legacy tag targets remain valid for different-tag contacts.

Tests added/changed: validation covers rejection of same-tag legacy targeting. Contact executor coverage explicitly verifies unrelated contacts do nothing, duplicate destroy rules destroy a participant once, and a later impulse targeting an already-destroyed participant does not execute. Focused contact/validation tests: 24 passed.

Playwright executed: `npx playwright test e2e/puzzle.e2e.ts --grep "keeps Bird patrol deterministic and executes configured Bird contacts" --workers=1`; passed (1 test, 18.4s).

Validation passed: `npm run typecheck`; `npm run lint`; `npm test` (12 files, 106 tests); `npm run format:check`; `npm run build`; `npx playwright test --list` (17 tests); `git diff --check`. Build produced only the existing Phaser chunk-size warning.

git status --short:
 M .codex/config.toml
 M docs/CODEX_PROMPTS.md
 M docs/PROJECT_STATE.md
 M docs/ROADMAP.md
 M src/game/PrototypeScene.ts
 M src/game/contactRules.ts
 M src/levels/levelTypes.ts
 M src/levels/validateLevel.ts
 M tests/contactRules.test.ts
 M tests/levelValidation.test.ts
?? docs/reviews/
?? s-20260812-1003-c7d4-response.md
?? src/game/headlessSimulation.ts
?? src/game/phaserMatter.d.ts
?? tests/headlessSimulation.test.ts
