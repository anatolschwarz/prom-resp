Updated the coworking workflow.

Files changed:
- `push-response.sh`: now requires, stages, and commits both `<session-token>-response.md` and `<session-token>-diff.patch`; it commits when either changes, then preserves `pull --rebase` and `push` behavior.
- `PROM_RESP_SETUP.md`: documents the two-artifact convention, direct Codex patch generation without `make-diff.sh`, the normal review-before-Contraption-Lab-commit flow, and exceptional full-code/review escalation.
- `s-20260812-0955-c7d4-diff.patch`: generated from all current uncommitted Contraption Lab changes, including tracked, untracked, and deleted paths with Git binary patch mode.

Checks run: `bash -n push-response.sh`; `git apply --numstat` on the generated patch (15 file diffs); documentation consistency search; `git diff --check` in `prom-resp`.
