Implemented Phase 2 milestone #28 (dynamic/toppling Block capability). Contraption-lab was not committed or pushed.

## Capability

A Block can now opt into being a Matter **dynamic rigid body** via two optional, declarative JSON fields — nothing character- or Painted-Block-specific:

- `dynamic?: boolean` — when `true`, the Block is a dynamic rigid body that can rotate/topple and transfer motion through collisions. Omitted or `false` keeps the historical static Block behavior, so every existing level is byte-for-byte unchanged.
- `rotation?: number` — initial rotation in radians (same convention as `RampDefinition`). Omitted means upright/axis-aligned (0). This is explicit gameplay geometry from the level JSON and is independent of any artwork silhouette.

Toppling and chaining are pure Matter physics acting on the Block's gameplay rectangle (`width`/`height`/`rotation`), not on any sprite. A dynamic Block rotates past its critical angle, settles on the floor, and pushes neighbouring dynamic Blocks — all deterministically.

## Design (and why it stays small — prompt item #10)

The capability is threaded through the existing seams rather than adding new machinery:

- **Schema/validation:** `BlockDefinition` extends `LevelPartDefinition` with the two optional fields; `validateLevel`'s `isBlock` guard gains two backward-compatible optional checks (`dynamic` boolean, `rotation` finite number). Legacy Blocks with neither field remain valid.
- **Rendering (`PrototypeScene`):** `createBlock` sets the body `isStatic: !dynamic` and applies the initial `rotation` directly to the newly attached Matter body. A dynamic Block's decorations (shadow/highlight/selection label) are re-synced from the live body each simulated step; `updateBlockSurface` rotates the decorative offsets. Both new code paths early-return for non-dynamic Blocks, and at rotation 0 the offset math is provably identical to the previous constants — so static Blocks render pixel-for-pixel as before.
- **Ownership/inventory:** untouched and orthogonal. `dynamic` composes with either `ownership: "fixed"` or `"player"`; `isPlayerPart`/`isEditablePart` are unchanged; level-defined Blocks (fixed or player) still do not consume tray Block inventory.
- **Reset (#5):** already recreates every Block from JSON via `createBlock`, which now restores position, `rotation`, and dynamic-body state (a fresh body at rest). No change needed beyond `createBlock` honoring the new fields.
- **Rerun (#6):** entering Edit is now an explicit rest-state boundary for dynamic Blocks: their linear and angular velocities are zeroed exactly once on the transition. The run-start snapshot therefore needs only transform data, and Rerun recreates a fresh rest body at the captured position/rotation. This is the smallest architecture-consistent fix for the review finding: no velocity state is added to the pure game-state snapshot.

**One fidelity fix beyond the JSON fields:** `captureRunLayout` previously recorded each Block's live `x,y` but the *definition* rotation. There is a wired **Edit** button that returns to edit mode after a run without a Reset, so a Block left toppled can reach a new run-start at a non-JSON angle. To satisfy "#6 restores the exact run-start rotation" in that reachable path, the Block snapshot now captures `block.shape.rotation` (the live angle), exactly as the ramp snapshot already does. For a Block that has not toppled, `shape.rotation === definition.rotation`, so the dominant path and all static Blocks are unchanged.

## Review follow-up: Rerun rest-state invariant

Review correctly identified that a dynamic Matter Block could carry nonzero linear/angular velocity from Running or Paused into Edit. `captureRunLayout` stores only transforms while `rerunFromSnapshot` creates fresh bodies at rest, so that path could not reproduce the actual next Run start.

`setEditSelection` now recognizes the transition into Edit and calls `normalizeDynamicBlocksForEdit`. It clears linear and angular velocity on dynamic Block Matter bodies only; static Blocks are untouched. The runtime regression test drives the real UI and scene path: it injects a dynamic Block into the Phaser scene, runs it with nonzero motion, enters Edit, confirms both velocities are zero, starts another Run, perturbs the body again, and clicks the actual Rerun button. A wrapper records the live restoration inside `rerunFromSnapshot` before the resumed simulation can advance, proving position, rotation, and zero linear/angular velocity match the captured run-start snapshot. The same test confirms Reset removes the injected non-JSON Block and restores the static JSON `guide-block` unchanged.

## Files changed

- `src/levels/levelTypes.ts` — `BlockDefinition` gains optional `dynamic` + `rotation`.
- `src/levels/validateLevel.ts` — `isBlock` validates the two optional fields.
- `src/game/headlessSimulation.ts` — Blocks honor `dynamic` (`isStatic: dynamic !== true`) and `rotation`; `SimulationResult` gains a `bodies` array (terminal id/tag/x/y/angle in registration order) so dynamic outcomes and deterministic replay are checkable headlessly.
- `src/game/PrototypeScene.ts` — dynamic-body creation + Matter-body initial rotation, per-step visual sync for dynamic Blocks, rotation-aware `updateBlockSurface`, live-rotation capture in `captureRunLayout`, and the Edit rest-state invariant.
- `tests/dynamicBlock.test.ts` — new focused suite (below).
- `e2e/puzzle.e2e.ts` — focused real Phaser scene/UI regression for the dynamic-Block Edit → Run → Rerun path, plus Reset/static-Block assertions.
- `docs/CODEX_PROMPTS.md` — appended this milestone's operational prompt per the existing convention.

## Tests added

`tests/dynamicBlock.test.ts` (12 cases): schema/validation (accepts + preserves `dynamic`/`rotation`; legacy Block valid; rejects non-boolean `dynamic` and non-finite `rotation`); ownership/inventory (orthogonal to ownership, classification unchanged, inventory not consumed); headless behavior (static default stays exactly in place; a leaning Block topples further and settles lower; motion transfers to a neighbouring dynamic Block via a `block:a`↔`block:b` contact; repeat runs are byte-identical; Reset reconstructs the exact initial JSON transform).

`e2e/puzzle.e2e.ts`: real-scene/UI coverage verifies Run → moving dynamic Block → Edit → Run → Rerun restores the captured transform at rest, Reset rebuilds JSON state, and the existing static Block remains static and unchanged.

## Validation

- `npm run typecheck` — pass.
- `npm run lint` — pass.
- `npm test` — 14 files, 124 tests pass (incl. the 12 new).
- `npm run format:check` — my files are Prettier-clean. The command reports two pre-existing warnings in `docs/design/phase2/reference-art/` (`assets-manifest.json`, `REFERENCE-ART-SESSION.md`), which are committed unmodified at HEAD (the Sunny Attic archive) and unrelated to #28.
- `npm run build` — pass (only the pre-existing Phaser chunk-size advisory).
- `git diff --check` — clean.
- Focused Playwright regression (`npx playwright test e2e/puzzle.e2e.ts --grep 'normalizes dynamic Blocks through Edit before runtime Rerun'`) — pass.

## Scope

No Sunny Attic sprite integration (#29), no Chapter-1 level changes (#30–#35), no new dependencies, no broad refactor, no object/character-specific branches, and no coupling of sprite silhouettes to Matter bodies. Contraption-lab is left uncommitted; the pre-existing untracked `docs/reviews/` and `s-20260812-1003-c7d4-response.md` are preserved and excluded from the patch.

git status --short:
 M docs/CODEX_PROMPTS.md
 M e2e/puzzle.e2e.ts
 M src/game/PrototypeScene.ts
 M src/game/headlessSimulation.ts
 M src/levels/levelTypes.ts
 M src/levels/validateLevel.ts
?? docs/reviews/
?? s-20260812-1003-c7d4-response.md
?? tests/dynamicBlock.test.ts
