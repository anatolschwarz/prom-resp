# Phase 2 #30 — Good Morning, Marble

Implemented L1 as built-in Basic catalog order 6 (`good-morning-marble-006`), preserving the five existing JSON levels. It uses the approved L1 corner, marble, plank, empty/goal teacups, and sparkle through the #29 Storybook registry/runtime-copy pipeline. Every current runtime `part`/`fx` copy, including the #29 mattress and teapot proof assets, is now a deterministic matte-removed, transparent, trimmed derivative; their approved reference originals remain untouched.

The level data owns all gameplay geometry: a sloped left shelf, explicit 430×24 Plank body, box platform, floor, and goal sensor. Storybook sprites are separately sized presentation only; opaque background art never generates collision bodies. Old primitive rendering remains the fallback for levels without `storybookPresentation`.

One `ramp: 1` inventory entry supplies the Plank. `trayRamp` and a reusable data-defined `rampSnapTargets` field provide a 170px forgiving snap without a level-id branch. Reset/Rerun continue to use the existing game-state/inventory semantics. Marble and teacup pokes animate only sprites/background alpha; no Matter or headless state changes occur.

`referenceSolutions` is optional validated content data, presently a named list of Ramp placements. L1 encodes `main-plank-bridge`: `l1-plank` at `(500, 353)`, `430×24`, rotation `0.26`. `simulateReferenceSolution` consumes that data directly. The reference solution solves deterministically at tick **435** with final `goal` for `morning-marble`; existing reference baselines remain 215 (Prototype), 244 (Down the Ramp), and 299 (Bridge the Gap).

Validation:

- `npm run typecheck` — pass
- `npm run lint` — pass
- `npm test` — pass, 132 tests
- `npm run storybook-assets:check` — pass; verifies all seven runtime part/fx PNGs have transparent outer pixels and the L1 environment remains opaque
- Targeted affected tests — 6 passed (`storybookAssets`, `goodMorningMarble`)
- `npm run build` — pass (existing Vite chunk-size warning only)
- `git diff --check` — #30 paths are clean; the command reports pre-existing unrelated trailing whitespace in the owner's modified `docs/ROADMAP.md`, which is excluded from this patch.
- Focused Storybook proof — `npx playwright test e2e/storybookAssets.e2e.ts --reporter=line` — **1 passed (10.6s)**, including transparent mattress/teapot texture corners and explicit independent bodies.
- Focused L1 Playwright coverage covers catalog selection, transparent runtime texture corners, snap placement, Run/goal/sparkle, Reset restoration, and console errors. The original required focused run passed in **24.2s**; the final post-pipeline run passed: `npx playwright test e2e/goodMorningMarble.e2e.ts --reporter=line` — **1 passed (41.7s)**.

Changed #30 paths include `docs/CODEX_PROMPTS.md`, Storybook runtime assets/provenance and manifest-driven processing/check scripts, level schema/validation/catalog, `PrototypeScene`, headless simulation, placement helper, focused unit/progression/catalog tests, the L1 JSON, and focused Storybook/L1 Playwright coverage.

Excluded/preserved unrelated work: `docs/ROADMAP.md` (owner change), `docs/ROADMAP.md.bak`, `docs/reviews/`, and `s-20260812-1003-c7d4-response.md`. Contraption Lab was not committed or pushed.

Review corrections: removed the duplicate `ramp.sprite?.destroy()` call. The visible Plank sprite is the interactive target and forwards its pointer event to the existing rectangle-based ramp drag handler; visual dimensions remain independent of Matter geometry. The real pointer path now uses the repository's established `mouse.move(..., { steps: 12 })` gesture. The final snap issue was an inverted fixed-support-overlap flag: authored snap targets may rest on fixed support blocks, while Balls, Ramps, and player blocks remain validated. The later asset-pipeline correction fixes the previously opaque mattress/teapot proof assets with the same manifest-driven processing used for every runtime part/fx; environments remain byte-for-byte opaque copies. The final L1 and Storybook proof tests pass without bypasses.
