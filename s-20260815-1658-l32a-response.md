# Phase 2 #32 — slice 2 actual-main routing regression

## Implementation

- Verified branch `phase2-32-tea-time`; `/home/anatolschwartz/code/contraption-lab-main` was not modified.
- Added [tests/mainRoutingIntegration.test.ts](/home/anatolschwartz/code/contraption-lab/tests/mainRoutingIntegration.test.ts). It loads the actual [src/main.ts](/home/anatolschwartz/code/contraption-lab/src/main.ts) module with mocked Phaser, Controls, and PrototypeScene dependencies, then invokes the exact action callback passed by `main.ts` to Controls.
- No production behavior changed and no slice 3 functionality was added.

## Automated tests/checks

- The actual-main test proves `begin-held-placement` renders state with the held Ramp and unchanged `trayRampCount` (`4`), while all six `scene.spawnTray*` spies remain untouched.
- It requires exactly one Controls render. Breaking/removing `main.ts`’s early return causes its existing held-placement route to apply once and the later main path to apply again, so this assertion fails.
- `npm test -- tests/mainRoutingIntegration.test.ts tests/mainActionRouting.test.ts tests/controls.test.ts tests/gameState.test.ts` — passed: 4 files, 34 tests.
- `npm run typecheck` — passed.
- `npm run lint` — passed.
- `git diff --check` — passed.

## Diff/artifact verification

- The regenerated patch contains the complete current slice 1/2 working-tree changes and this actual-main regression test.
- `git apply --check --reverse --whitespace=nowarn` passed against the current tree.
- The prompt was appended verbatim to [docs/CODEX_PROMPTS.md](/home/anatolschwartz/code/contraption-lab/docs/CODEX_PROMPTS.md).

## Unresolved issues

Slice 3 remains out of scope and unimplemented.

## Next integration boundary

No further work in slice 2.
