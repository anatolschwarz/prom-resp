# Phase 2 #32 — held placement slice 1

## Implementation

- Added required `GameState.heldPlacement`, defaulting to `null`.
- Added generic `begin-held-placement`, `update-held-placement`, `commit-held-placement`, and `cancel-held-placement` actions in [src/state/gameState.ts](/home/anatolschwartz/code/contraption-lab/src/state/gameState.ts).
- Held parts use the existing registry inventory keys and a provisional id plus position/rotation transform. Begin requires matching inventory but does not decrement it. Update changes only the held transform. Cancel clears the held placement without changing inventory. Commit decrements exactly once, selects the provisional id, and clears the held placement; a second commit is inert.
- No Controls, scene, rendering/input, Matter/contact, Tea Time, reset wiring, or browser/E2E code changed.

## Automated tests/checks

- `npm test -- tests/gameState.test.ts` — passed: 1 file, 28 tests.
- `npm run typecheck` — passed.
- `npm run lint` — passed.
- `git diff --check` — passed.

## Diff/artifact verification

- Implementation: [src/state/gameState.ts](/home/anatolschwartz/code/contraption-lab/src/state/gameState.ts).
- Focused tests: [tests/gameState.test.ts](/home/anatolschwartz/code/contraption-lab/tests/gameState.test.ts).
- The regenerated handoff patch contains the held-placement state/action and test changes reported above, plus this prompt-log append. It excludes unrelated current-tree files.

## Unresolved issues

UI/scene integration is intentionally absent: no provisional render, pointer updates, Q/E wiring, runtime object creation, or browser verification exists in this slice.

## Next integration boundary

Wire the existing Controls/main/PrototypeScene inventory path to these reducer actions, then add provisional rendering and input handling in a separate slice.
