# Phase 2 #32 — Browser Matter correction

Fixed the browser/runtime divergence in Tea Time.

- Phaser now dispatches every `collisionstart` pair, matching the headless Matter loop; the previous callback handled only the final pair in a tick.
- The Teapot rule is now generic for a Ball–Teapot contact. It no longer depends on the headless reference-only `l3-teapot` ID, so the placed `tray-teapot-1` sensor redirects the marble.
- Tray Teapot physics geometry now matches the explicit reference geometry; its Storybook sprite remains visual-only.
- The under-table cup accepts the intended, downward/near-vertical entry and rejects the direct horizontal pass. This is declarative goal-entry data shared by browser and headless paths, not an L3 code branch.
- Browser coverage uses actual Phaser/Matter pairs and confirms the Teapot contact, authored redirect velocity, cup success, and no-Teapot non-solution.
- Appended the correction prompt verbatim to `docs/CODEX_PROMPTS.md`.

Validation:

- `npx vitest run tests/teaTime.test.ts` — 7 passed.
- Focused `e2e/teaTime.e2e.ts` — both tests passed (run individually because each Playwright worker run is close to the execution-cell time limit).
- `npm run typecheck` — passed.
- `npm run lint` — passed.
- `npm test` — 18 files / 144 tests passed.
- `npm run build` — passed; existing Vite chunk-size warning remains.
- `git diff --check` — passed.
- `npm run format:check` reports pre-existing formatting failures in unrelated `docs/design/phase2/reference-art/assets-manifest.json`, `docs/design/phase2/reference-art/REFERENCE-ART-SESSION.md`, and `docs/ROADMAP.md`.

Contraption Lab was not committed or pushed. Unrelated owner work was preserved.
