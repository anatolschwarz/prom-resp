# Phase 2 #32 — held placement slice 2

## Implementation

- Preserved slice 1 state primitives in [src/state/gameState.ts](/home/anatolschwartz/code/contraption-lab/src/state/gameState.ts).
- Changed every Parts inventory button in [src/ui/Controls.ts](/home/anatolschwartz/code/contraption-lab/src/ui/Controls.ts) to dispatch `begin-held-placement` with a unique provisional id and zero transform.
- Added an explicit `begin-held-placement` route in [src/main.ts](/home/anatolschwartz/code/contraption-lab/src/main.ts). It transitions state and returns before every `scene.spawnTray*` branch; no runtime, render, Matter, or contact object is created.
- Inventory is unchanged on click because the slice 1 reducer decrements only on `commit-held-placement`.
- No provisional rendering, pointer-following, held rotation, click-to-commit, cancellation input, or lifecycle cleanup was added.

## Automated tests/checks

- [tests/controls.test.ts](/home/anatolschwartz/code/contraption-lab/tests/controls.test.ts) covers Ramp click dispatch, all six placeable inventory parts, unchanged inventory, absence of `spawn-tray-*` actions, and the unchanged Run control action.
- `npm test -- tests/controls.test.ts tests/gameState.test.ts` — passed: 2 files, 32 tests.
- `npm run typecheck` — passed.
- `npm run lint` — passed.
- `git diff --check` — passed.

## Diff/artifact verification

- The regenerated patch contains `docs/CODEX_PROMPTS.md`, `src/main.ts`, `src/state/gameState.ts`, `src/ui/Controls.ts`, `tests/gameState.test.ts`, and `tests/controls.test.ts`.
- `git apply --check --reverse --whitespace=nowarn` passed against the current tree, including the untracked focused test.
- The patch contains every implementation/test file reported here; no unrelated working-tree file is included.

## Unresolved issues

Held placement has no scene representation or player interaction yet. Existing `spawn-tray-*` behavior remains for existing non-inventory callers.

## Next integration boundary

Add provisional rendering and pointer transform updates, without committing inventory or creating final runtime objects until a later commit slice.
