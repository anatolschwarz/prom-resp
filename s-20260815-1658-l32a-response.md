# Phase 2 #32 — Slice 4 committed-Ramp transform

`phase2-32-tea-time` is active. Checkpoint `d77c55387bfada5a81d387cbe020e2999da7610b` is an ancestor of `HEAD`. `/home/anatolschwartz/code/contraption-lab-main` remains clean and was not modified. No Contraption Lab commit or push was made.

## Diagnosis and fix

- The held Ramp immediately before the clear-target click was `(539.8830409356725, 299.81667394151026, 0 rad)`: the precise canvas conversion of requested DOM target `(540, 300)`.
- The commit callback receives and passes that same held transform to `commitHeldPlacement`; `createRamp` initially creates the runtime visual and Matter-backed shape at that anchor.
- The mismatch occurred afterward during state application. `setRampTransforms` supplied the committed Ramp's definition transform because no persisted ramp transform exists yet, then called `updateRampTransform(..., notify=false)`. That method nevertheless snapped it to Tea Time's nearby snap target `(490, 307, 0.17 rad)` (tolerance `145`). Thus production, not the test, changed the committed transform.
- `updateRampTransform` now snaps only direct editor interactions (`notify=true`). State application (`notify=false`) preserves the recorded/runtime placement anchor. Existing editor drag snapping remains unchanged.
- The browser regression now directly verifies the held, runtime shape, visual sprite, and Matter-body anchors/angles after commit. It keeps the existing target assertion; this is not a tolerance-only test change.

After the fix, the committed runtime shape, sprite, and Matter body all match the held anchor `(539.8830409356725, 299.81667394151026, 0 rad)` within the test's explicit `<=1 px` canvas-coordinate tolerance and `<=0.01 rad` angle tolerance. The tolerance accounts for DOM-to-canvas conversion; the runtime anchor is not intentionally offset.

`createRamp` uses a common center anchor for its sprite and collision shape. Tea Time currently renders the Ramp artwork at `230 x 52`, while its Matter rectangle is `230 x 24`; the hidden shadow is locally offset `(3, 4)` and the hidden highlight is offset upward by `5.28 px`. Those art-local differences do not cause the committed-transform mismatch. They do explain the observed collision surface appearing about `14 px` below the artwork's top edge. This geometry/art alignment remains unchanged because this run is limited to the Slice 4 committed-transform defect.

## Automated tests and checks

- `npm test -- tests/mainRoutingIntegration.test.ts tests/mainActionRouting.test.ts tests/gameState.test.ts` — PASS, 3 files / 30 tests.
- `npx playwright test e2e/teaTime.e2e.ts --grep "keeps an invalid held Ramp and commits a valid Ramp with normal registration" --reporter=line` — PASS, 1 test (`test-results/.last-run.json`: `status: passed`).
- `npm run typecheck` — PASS.
- `npm run lint` — PASS.
- `git diff --check` — PASS.

## Browser verification

The focused Tea Time test verifies invalid held placement remains held with no runtime/Matter/contact changes, then verifies valid placement clears held state and preview, decrements inventory once, creates exactly one runtime Ramp and Matter body, registers its contact, preserves selection, and preserves held/runtime/visual/Matter anchor alignment. A repeated click does not create or decrement again.

## Unresolved issues

- Ramp artwork (`52 px` high) and Matter geometry (`24 px` high) remain visually misaligned; not changed in this scoped transform fix.
- Slice 5 cancellation, held Q/E rotation, and lifecycle cleanup remain unimplemented.

## Manual verification

Not required for this fix. The focused real-browser regression passed.

## Artifact verification

`s-20260815-1658-l32a-diff.patch` is regenerated from the current scoped #32 working-tree diff. It includes `docs/CODEX_PROMPTS.md`, `e2e/teaTime.e2e.ts`, `src/game/PrototypeScene.ts`, `src/main.ts`, and `tests/mainRoutingIntegration.test.ts`; it excludes unrelated `README.md`, backup/review files, and the unrelated response artifact.
