# Phase 2 #32 — L3 “Tea Time” correction

Corrected L3’s runtime layout so the cup is beneath the central doll table, with the marble travelling across the tabletop into the Teapot. The declarative, reusable Teapot sensor/redirect remains unchanged; Phaser and headless simulation share the same semantics and explicit gameplay geometry.

Both named deterministic routes remain encoded and verified:

- `normal-plank-pour-through`
- `star-no-plank-direct-roll`

The focused browser coverage selects L3, confirms the under-table cup geometry, places the Teapot and Plank, solves, reruns, and resets inventory. The correction prompt is appended verbatim at the end of `docs/CODEX_PROMPTS.md`.

Validation:

- Focused affected Vitest: 65 tests passed.
- `npm run typecheck` — passed.
- `npm run lint` — passed.
- `npm test` — 18 files / 143 tests passed.
- `npx playwright test e2e/teaTime.e2e.ts` — passed.
- `npm run build` — passed (existing Vite chunk-size warning only).
- `git diff --check` — passed.

Unrelated owner/untracked work was preserved. Contraption Lab was not committed or pushed.
