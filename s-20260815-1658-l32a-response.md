# Phase 2 #32 — Initial marble fall correction

Changed only Tea Time's initial marble position: `(250, 235)` → `(250, 220)`.

This clears the rotated tabletop at Run start without assigning any velocity or changing Teapot, cup, redirect, physics, reference solutions, or test code.

Actual Phaser/Matter browser trace after Run:

| Tick | Position `(x, y)` | Velocity `(vx, vy)` |
| --- | --- | --- |
| 0 | `(250.000, 220.000)` | `(0.000, 0.000)` |
| 1 | `(250.000, 220.278)` | `(0.000, 0.278)` |
| 2 | `(250.000, 220.831)` | `(0.000, 0.553)` |
| 3 | `(250.000, 221.656)` | `(0.000, 0.825)` |
| 4 | `(250.000, 222.750)` | `(0.000, 1.095)` |
| 5 | `(250.000, 224.112)` | `(0.000, 1.361)` |
| 6 | `(250.204, 224.551)` | `(0.193, 0.034)` |

The first physical contact occurs at tick 6: `block:tabletop-left-geometry ↔ ball:tea-marble`. X is exactly unchanged and `vx=0` through tick 5; the marble initially falls only under gravity. Rightward motion begins only after the legitimate tabletop contact.

Validation:

- `npx vitest run tests/teaTime.test.ts` — 7 passed.
- Browser runtime trace — passed as recorded above.
- Focused `e2e/teaTime.e2e.ts` was invoked; the execution cell reached its time limit before producing the final summary.
- `npm run typecheck` — passed.
- `npm run lint` — passed.
- `git diff --check` — passed.

The correction prompt was appended verbatim to `docs/CODEX_PROMPTS.md`. Contraption Lab was not committed or pushed; unrelated work was preserved.
