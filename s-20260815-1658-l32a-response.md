# Phase 2 #32 — Visible marble diagnostic

I could not reproduce a body/sprite contradiction in the current browser UI. No gameplay, geometry, physics, test, or reference-solution changes were made.

Tested the actual built-in Tea Time route at `http://localhost:5173/` with `levelId: tea-time-008` and title `Tea Time`.

| Tick | Matter body `(x, y)` | Velocity `(vx, vy)` | Rendered sprite `(x, y)` |
| --- | --- | --- | --- |
| 0 | `(250.000, 220.000)` | `(0.000, 0.000)` | `(250.000, 220.000)` |
| 1 | `(250.000, 220.278)` | `(0.000, 0.278)` | `(250.000, 220.278)` |
| 2 | `(250.000, 220.831)` | `(0.000, 0.553)` | `(250.000, 220.831)` |
| 3 | `(250.000, 221.656)` | `(0.000, 0.825)` | `(250.000, 221.656)` |
| 4 | `(250.000, 222.750)` | `(0.000, 1.095)` | `(250.000, 222.750)` |
| 5 | `(250.000, 224.112)` | `(0.000, 1.361)` | `(250.000, 224.112)` |
| 6 | `(250.204, 224.551)` | `(0.193, 0.034)` | `(250.204, 224.551)` |

Sprite/body alignment was exact in every sample: `spriteX - bodyX = 0`, `spriteY - bodyY = 0`.

The first collision is tick 6: `block:tabletop-left-geometry ↔ ball:tea-marble`. The sprite therefore falls only 4.551 px over the first five 60 Hz ticks (about 83 ms) before contacting the tabletop and beginning to move right. With a 56 px marble sprite, that short visual drop can appear stationary before the roll begins.

There is no independent rendered-marble motion source. `PrototypeScene.update()` calls `updateBallHighlights()` after `matter.world.step()`, and `updateBallHighlight()` performs `ball.sprite.setPosition(ball.shape.x, ball.shape.y)`. This synchronizes the sprite to the Matter body; it does not overwrite the body or prevent falling.

Conclusion: in the current browser scene, the visible marble does fall and remains exactly synchronized with its Matter body. The reported contradiction is not present in this reproduction. The diagnostic prompt was appended verbatim to `docs/CODEX_PROMPTS.md`. Contraption Lab was not committed or pushed.
