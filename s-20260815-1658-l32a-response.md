# Phase 2 #32 — L3 “Tea Time”

Implemented `tea-time-008`, the eighth Basic puzzle: a tea-party corner with a doll table, cup below the table, one Teapot and one Plank.

The Teapot is a reusable static sensor with explicit level-authored rectangle geometry. Its ball redirect is a standard declarative contact rule (`ball` + `teapot`), preserving the ball's speed while sending it down-left; Phaser and the headless runner use the same rule semantics. Sprite dimensions remain presentation-only.

L3 has two deterministic named reference routes:

- `normal-plank-pour-through`
- `star-no-plank-direct-roll`

Both solve headlessly and use the Teapot redirect; the star route has no Ramp placement. Teapot inventory, removal, Run snapshot/Rerun, Reset, palette support, level schema validation, catalog/progression, and focused browser coverage are included. The approved `corner_L3_v1.png` environment was copied through the existing Storybook pipeline; it remains opaque, while existing part/fx runtime assets retain their transparency checks.

Validation:

- `npm run storybook-assets:check` — passed.
- `npm run typecheck` — passed.
- `npm run lint` — passed.
- `npm test` — 18 files / 142 tests passed.
- `npx playwright test e2e/teaTime.e2e.ts` — 1 passed.
- L3-owned Prettier check — passed.
- `npm run build` — passed (existing Vite chunk-size warning only).
- `git diff --check` — passed.
- Repository-wide `npm run format:check` is blocked only by pre-existing, unrelated formatting in `docs/design/phase2/reference-art/assets-manifest.json`, `docs/design/phase2/reference-art/REFERENCE-ART-SESSION.md`, and owner-owned `docs/ROADMAP.md`; none were modified.

The full operational prompt was appended verbatim to `docs/CODEX_PROMPTS.md`. Unrelated owner/untracked work was preserved. Contraption Lab was not committed or pushed.
