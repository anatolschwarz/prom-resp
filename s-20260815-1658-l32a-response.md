# Phase 2 #32 — tabletop alignment correction

Corrected Tea Time's fixed tabletop collision rectangle to the measured visible tabletop:

| | Before | After |
| --- | --- | --- |
| Center | `(380, 280)` | `(483, 378)` |
| Size | `350 × 30` | `350 × 36` |
| Rotation | `0.17 rad` | `0.01 rad` |
| World bounds | x `204.985–555.015`, y `235.609–324.391` | x `307.829–658.171`, y `358.251–397.749` |

The measured visible tabletop surface spans approximately `(308, 360)` to `(658, 362)`. The corrected rectangle's upper corners are `(308.189, 358.251)` and `(658.171, 361.751)`.

The original marble position was invalid after aligning the tabletop: x=`250` is left of the visible table. It now starts at `(400, 300)`, visibly above the tabletop. In the actual browser scene, ticks 0–6 show x=`400` and sprite/body alignment throughout while y advances from `300.000` to `305.737`; no tabletop contact occurs and vx remains zero. The first headless tabletop contact is tick 18 at the art-aligned surface.

Targeted Tea Time Vitest exposes an existing route dependency that this correction cannot resolve without prohibited changes: both reference routes depend on the prior invisible 0.17-radian slope and gap at the table's right side. With continuous art-aligned support, the fixed Teapot redirect lands back on the tabletop rather than reaching the cup. The test therefore currently fails its two reference-solution assertions. I did not change Teapot redirect/contact behavior, cup behavior, or add a level-specific branch.

`npm run typecheck`, `npm run lint`, and `git diff --check` passed. The full operational prompt was appended verbatim to `docs/CODEX_PROMPTS.md`. No Contraption Lab commit or push was made.

## Latest requested initial-position update

Changed only the Tea Time marble's initial y-coordinate from `300` to `180`; x remains `400`. No tabletop, Teapot, cup, physics, or reference-route data changed.

Actual Phaser/Matter browser trace:

| Tick | Matter body / sprite position | Velocity |
| --- | --- | --- |
| 0 | `(400.000, 180.000)` | `(0.000, 0.000)` |
| 1 | `(400.000, 180.278)` | `(0.000, 0.278)` |
| 3 | `(400.000, 181.656)` | `(0.000, 0.825)` |
| 6 | `(400.000, 185.737)` | `(0.000, 1.626)` |

The body, Phaser shape, and rendered sprite had identical positions at every sampled tick. No contact occurred through tick 6, and x remained exactly `400` while the marble fell under gravity.
