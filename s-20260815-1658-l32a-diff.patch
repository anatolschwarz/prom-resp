diff --git a/docs/CODEX_PROMPTS.md b/docs/CODEX_PROMPTS.md
index 595e647..bc5924b 100644
--- a/docs/CODEX_PROMPTS.md
+++ b/docs/CODEX_PROMPTS.md
@@ -6701,3 +6701,166 @@ IMPORTANT: append THIS FULL ORIGINAL OPERATIONAL PROMPT VERBATIM to docs/CODEX_P
 Do not paraphrase, reconstruct, summarize, or omit any part of it.
 
 Then STOP.
+
+Continue Phase 2 #32 from checkpoint:
+
+f19d69890e8170f986c992b6c21b169308a244d2
+
+Work ONLY in:
+/home/anatolschwartz/code/contraption-lab
+
+Verify:
+- branch = phase2-32-tea-time
+- checkpoint above is an ancestor of HEAD
+- do NOT modify /home/anatolschwartz/code/contraption-lab-main
+
+Session token:
+s-20260815-1658-l32a
+
+Implement ONLY held-placement slice 3:
+
+PROVISIONAL VISUAL + POINTER FOLLOWING.
+
+Required behavior:
+
+1. When heldPlacement is active:
+- render exactly one provisional visual for that part;
+- use the normal part artwork/visual representation where practical;
+- do NOT create the final runtime part;
+- do NOT create a Matter body;
+- do NOT register contacts/collisions.
+
+2. Pointer movement:
+- updates heldPlacement position through `update-held-placement`;
+- provisional visual follows the held transform;
+- overlapping/invalid positions remain held;
+- do not reject or cancel because the cursor is currently over an invalid location.
+
+3. State/inventory:
+- inventory remains unchanged;
+- heldPlacement remains active;
+- existing committed-part selection/drag behavior remains unchanged.
+
+4. Cleanup:
+- state re-renders must not create duplicate provisional visuals;
+- when heldPlacement becomes null, destroy/remove the provisional visual cleanly.
+
+Apply GENERICALLY to all applicable inventory-placeable parts.
+Use Tea Time Ramp/Teapot only as regression cases.
+
+Do NOT implement yet:
+- click-to-commit;
+- Escape/right-click cancel;
+- Q/E held rotation;
+- final runtime/Matter creation;
+- broad Reset/puzzle-switch lifecycle changes;
+- Tea Time reference-route/physics fixes;
+- Parts layout changes;
+- Teapot opacity;
+- unrelated collision/reset fixes.
+
+Automated verification:
+
+Focused tests:
+- heldPlacement creates exactly one provisional visual;
+- pointer movement updates held position;
+- provisional visual follows;
+- inventory does not decrement;
+- no spawnTray*/runtime creation;
+- no Matter/contact registration;
+- clearing heldPlacement removes the provisional visual.
+
+Browser regression:
+- enter Edit mode;
+- open Parts;
+- click Ramp;
+- provisional Ramp becomes visible;
+- move pointer;
+- provisional Ramp follows;
+- inventory remains 1;
+- no committed/runtime Ramp is created.
+
+Use bounded browser timeouts and cleanly close browser/context/process handles.
+
+Run:
+- focused relevant tests
+- focused Playwright regression
+- npm run typecheck
+- npm run lint
+- git diff --check
+
+Do not commit or push Contraption Lab.
+
+Create/update:
+- s-20260815-1658-l32a-response.md
+- s-20260815-1658-l32a-diff.patch
+
+Response must separate:
+- implementation
+- automated tests
+- browser verification
+- unresolved issues
+- manual verification requirement, if any
+
+Handoff using exactly:
+/home/anatolschwartz/code/prom-resp/push-response.sh s-20260815-1658-l32a
+
+IMPORTANT: append THIS FULL ORIGINAL OPERATIONAL PROMPT VERBATIM to docs/CODEX_PROMPTS.md.
+Do not paraphrase, reconstruct, summarize, or omit any part of it.
+
+Then STOP.
+
+Continue Phase 2 #32.
+
+Work ONLY in:
+/home/anatolschwartz/code/contraption-lab
+
+Verify:
+- branch = phase2-32-tea-time
+- do NOT modify /home/anatolschwartz/code/contraption-lab-main
+
+Session token:
+s-20260815-1658-l32a
+
+Fix ONLY the failing Slice 3 Playwright assertion.
+
+Observed real browser result:
+- requested pointer target: (540, 300)
+- heldPosition: (539.8830409356725, 299.81667394151026)
+- previewPosition: (539.8830409356725, 299.81667394151026)
+- previewCount: 1
+- trayRampCount: 1
+
+The production behavior is correct. The E2E incorrectly requires exact integer canvas coordinates.
+
+Required test correction:
+- do NOT change production code;
+- replace exact target coordinate equality with a small explicit tolerance suitable for DOM/canvas coordinate conversion (<= 1px);
+- still assert heldPosition and previewPosition match each other closely;
+- preserve assertions that previewCount = 1 and inventory remains unchanged;
+- do not weaken unrelated assertions.
+
+Run:
+npx playwright test e2e/teaTime.e2e.ts --grep "renders and moves a held Ramp preview without runtime registration" --reporter=line
+
+Also run:
+- npm run typecheck
+- npm run lint
+- git diff --check
+
+Do NOT implement Slice 4 yet.
+Do NOT commit or push Contraption Lab.
+
+Create/update:
+- s-20260815-1658-l32a-response.md
+- s-20260815-1658-l32a-diff.patch
+
+Response must report the exact assertion change and exact test results.
+
+Handoff using exactly:
+/home/anatolschwartz/code/prom-resp/push-response.sh s-20260815-1658-l32a
+
+IMPORTANT: append THIS FULL ORIGINAL OPERATIONAL PROMPT VERBATIM to docs/CODEX_PROMPTS.md.
+Do not paraphrase, reconstruct, summarize, or omit any part of it.
+
+Then STOP.
diff --git a/e2e/teaTime.e2e.ts b/e2e/teaTime.e2e.ts
index 8e3dd39..8415e2e 100644
--- a/e2e/teaTime.e2e.ts
+++ b/e2e/teaTime.e2e.ts
@@ -121,6 +121,146 @@ test("opens Parts below the controls and starts held Teapot placement", async ({
     });
 });
 
+test("renders and moves a held Ramp preview without runtime registration", async ({
+  page,
+}) => {
+  test.setTimeout(45_000);
+  await openTeaTime(page);
+  const parts = page.locator("#parts-palette");
+  if (!(await parts.isVisible()))
+    await page.locator("#parts-palette-toggle").click();
+  const canvas = page.locator("#game-container canvas");
+  const canvasBox = await canvas.boundingBox();
+  if (!canvasBox) throw new Error("Tea Time canvas has no bounds.");
+
+  const before = await page.evaluate(() => {
+    const scene =
+      window.__contraptionLabTest?.getPrototypeScene() as unknown as {
+        contactObjects: Map<unknown, unknown>;
+        matter: { world: { engine: { world: { bodies: unknown[] } } } };
+      };
+    return {
+      contactCount: scene.contactObjects.size,
+      matterBodyCount: scene.matter.world.engine.world.bodies.length,
+    };
+  });
+
+  await page.locator("#tray-ramp-button").click();
+  await expect
+    .poll(() =>
+      page.evaluate(() => {
+        const state = window.__contraptionLabTest?.getGameState();
+        const scene =
+          window.__contraptionLabTest?.getPrototypeScene() as unknown as {
+            contactObjects: Map<unknown, unknown>;
+            heldPlacementVisual?: { x: number; y: number };
+            matter: { world: { engine: { world: { bodies: unknown[] } } } };
+            ramps: Map<string, unknown>;
+            children: { list: Array<{ name: string }> };
+          };
+        return {
+          contactCount: scene.contactObjects.size,
+          heldPlacement: state?.heldPlacement,
+          matterBodyCount: scene.matter.world.engine.world.bodies.length,
+          previewCount: scene.children.list.filter((child) =>
+            child.name.startsWith("held-placement:"),
+          ).length,
+          previewPosition: scene.heldPlacementVisual
+            ? { x: scene.heldPlacementVisual.x, y: scene.heldPlacementVisual.y }
+            : null,
+          runtimeRampCount: scene.ramps.size,
+          trayRampCount: state?.trayRampCount,
+        };
+      }),
+    )
+    .toMatchObject({
+      contactCount: before.contactCount,
+      heldPlacement: { part: "ramp" },
+      matterBodyCount: before.matterBodyCount,
+      previewCount: 1,
+      runtimeRampCount: 0,
+      trayRampCount: 1,
+    });
+
+  const target = { x: 540, y: 300 };
+  const canvasCoordinateTolerance = 1;
+  await canvas.hover({
+    position: {
+      x: (target.x / 960) * canvasBox.width,
+      y: (target.y / 540) * canvasBox.height,
+    },
+  });
+  await expect
+    .poll(() =>
+      page.evaluate((tolerance) => {
+        const state = window.__contraptionLabTest?.getGameState();
+        const heldPosition = state?.heldPlacement?.transform.position;
+        const scene =
+          window.__contraptionLabTest?.getPrototypeScene() as unknown as {
+            heldPlacementVisual?: { x: number; y: number };
+            children: { list: Array<{ name: string }> };
+          };
+        return {
+          heldMatchesPreview:
+            heldPosition !== undefined &&
+            scene.heldPlacementVisual !== undefined &&
+            Math.abs(heldPosition.x - scene.heldPlacementVisual.x) <= 0.01 &&
+            Math.abs(heldPosition.y - scene.heldPlacementVisual.y) <= 0.01,
+          heldWithinTargetTolerance:
+            heldPosition !== undefined &&
+            Math.abs(heldPosition.x - 540) <= tolerance &&
+            Math.abs(heldPosition.y - 300) <= tolerance,
+          previewCount: scene.children.list.filter((child) =>
+            child.name.startsWith("held-placement:"),
+          ).length,
+          previewWithinTargetTolerance:
+            scene.heldPlacementVisual !== undefined &&
+            Math.abs(scene.heldPlacementVisual.x - 540) <= tolerance &&
+            Math.abs(scene.heldPlacementVisual.y - 300) <= tolerance,
+          trayRampCount: state?.trayRampCount,
+        };
+      }, canvasCoordinateTolerance),
+    )
+    .toEqual({
+      heldMatchesPreview: true,
+      heldWithinTargetTolerance: true,
+      previewCount: 1,
+      previewWithinTargetTolerance: true,
+      trayRampCount: 1,
+    });
+
+  await page.evaluate(() =>
+    window.__contraptionLabTest?.dispatchAction({
+      type: "cancel-held-placement",
+    }),
+  );
+  await expect
+    .poll(() =>
+      page.evaluate(() => {
+        const state = window.__contraptionLabTest?.getGameState();
+        const scene =
+          window.__contraptionLabTest?.getPrototypeScene() as unknown as {
+            children: { list: Array<{ name: string }> };
+            ramps: Map<string, unknown>;
+          };
+        return {
+          heldPlacement: state?.heldPlacement,
+          previewCount: scene.children.list.filter((child) =>
+            child.name.startsWith("held-placement:"),
+          ).length,
+          runtimeRampCount: scene.ramps.size,
+          trayRampCount: state?.trayRampCount,
+        };
+      }),
+    )
+    .toEqual({
+      heldPlacement: null,
+      previewCount: 0,
+      runtimeRampCount: 0,
+      trayRampCount: 1,
+    });
+});
+
 test("does not send the marble to the cup without a Teapot contact", async ({
   page,
 }) => {
diff --git a/src/game/PrototypeScene.ts b/src/game/PrototypeScene.ts
index 15f72b5..7bf26ff 100644
--- a/src/game/PrototypeScene.ts
+++ b/src/game/PrototypeScene.ts
@@ -48,6 +48,7 @@ import {
 import type {
   BallTransform,
   BlockTransform,
+  HeldPlacement,
   RampTransform,
 } from "../state/gameState";
 import {
@@ -268,6 +269,8 @@ export class PrototypeScene extends Phaser.Scene {
   >();
   private readonly ramps = new Map<string, EditableRamp>();
   private readonly storybookObjects = new Map<string, StorybookObject>();
+  private activeHeldPlacement: HeldPlacement | null = null;
+  private heldPlacementVisual?: Phaser.GameObjects.Container;
   private runSnapshot?: SceneRunSnapshot;
   private simulationAccumulatorMs = 0;
   private simulationRunning = false;
@@ -294,6 +297,9 @@ export class PrototypeScene extends Phaser.Scene {
       ballId: string,
       transform: BallTransform,
     ) => void,
+    private readonly onHeldPlacementTransformChange: (
+      transform: RampTransform,
+    ) => void,
     private readonly onComponentRemove: (
       componentId: string,
       returnsTrayPart:
@@ -346,15 +352,21 @@ export class PrototypeScene extends Phaser.Scene {
       },
     );
     this.input.on(Phaser.Input.Events.POINTER_DOWN, () => {
-      if (!this.editInteractionEnabled) return;
+      if (!this.editInteractionEnabled || this.activeHeldPlacement) return;
       this.onSelectionChange(null);
     });
     this.input.on(
       Phaser.Input.Events.POINTER_MOVE,
       (pointer: Phaser.Input.Pointer) => {
-        if (!this.editInteractionEnabled || !this.drag || !pointer.isDown) {
+        if (!this.editInteractionEnabled) return;
+        if (this.activeHeldPlacement) {
+          this.onHeldPlacementTransformChange({
+            position: { x: pointer.worldX, y: pointer.worldY },
+            rotation: this.activeHeldPlacement.transform.rotation,
+          });
           return;
         }
+        if (!this.drag || !pointer.isDown) return;
         if (
           !this.dragMoved &&
           isClickMovementWithinTolerance(this.drag.pointerStart, {
@@ -510,6 +522,38 @@ export class PrototypeScene extends Phaser.Scene {
     this.updateSelectionDisplay();
   }
 
+  setHeldPlacement(placement: HeldPlacement | null): void {
+    const replacesVisual =
+      this.activeHeldPlacement?.id !== placement?.id ||
+      this.activeHeldPlacement?.part !== placement?.part;
+    this.activeHeldPlacement = placement
+      ? {
+          ...placement,
+          transform: {
+            position: { ...placement.transform.position },
+            rotation: placement.transform.rotation,
+          },
+        }
+      : null;
+
+    if (!placement) {
+      this.heldPlacementVisual?.destroy(true);
+      this.heldPlacementVisual = undefined;
+      return;
+    }
+
+    if (replacesVisual) {
+      this.heldPlacementVisual?.destroy(true);
+      this.heldPlacementVisual = this.createHeldPlacementVisual(placement);
+    }
+    this.heldPlacementVisual
+      ?.setPosition(
+        placement.transform.position.x,
+        placement.transform.position.y,
+      )
+      .setRotation(placement.transform.rotation);
+  }
+
   setRampTransforms(transforms: Readonly<Record<string, RampTransform>>): void {
     for (const ramp of this.ramps.values()) {
       if (!ramp.editable) continue;
@@ -548,6 +592,108 @@ export class PrototypeScene extends Phaser.Scene {
     }
   }
 
+  private createHeldPlacementVisual(
+    placement: Readonly<HeldPlacement>,
+  ): Phaser.GameObjects.Container {
+    const presentation = this.level.storybookPresentation;
+    const render =
+      placement.part === "ball"
+        ? presentation?.ball
+        : placement.part === "ramp"
+          ? presentation?.ramp
+          : placement.part === "mattress"
+            ? presentation?.mattress
+            : placement.part === "teapot"
+              ? presentation?.teapot
+              : undefined;
+    const children: Phaser.GameObjects.GameObject[] = [];
+
+    if (render) {
+      children.push(
+        this.add
+          .image(0, 0, getStorybookAssetById(render.asset).textureKey)
+          .setDisplaySize(render.width, render.height),
+      );
+    } else if (placement.part === "ball") {
+      const radius =
+        this.level.balls.find((ball) => ball.ownership === "player")?.radius ??
+        18;
+      children.push(
+        this.add
+          .circle(0, 0, radius, BALL_FILL_COLOR)
+          .setStrokeStyle(4, BALL_STROKE_COLOR),
+        this.add.circle(
+          -radius * 0.32,
+          -radius * 0.34,
+          Math.max(3, radius * 0.22),
+          BALL_HIGHLIGHT_COLOR,
+          0.9,
+        ),
+      );
+    } else if (placement.part === "bird") {
+      children.push(
+        this.add.ellipse(-3, 1, 30, 18, BIRD_BODY_COLOR),
+        this.add.ellipse(-7, -2, 13, 8, BIRD_HIGHLIGHT_COLOR, 0.82),
+        this.add.circle(11, -7, 9, BIRD_BODY_COLOR),
+        this.add.triangle(22, -6, -4, -5, 7, 0, -4, 5, BIRD_BEAK_COLOR),
+      );
+    } else {
+      const geometry = this.getHeldPlacementGeometry(placement.part);
+      children.push(
+        this.add
+          .rectangle(0, 0, geometry.width, geometry.height, geometry.fill)
+          .setStrokeStyle(4, geometry.stroke),
+      );
+    }
+
+    return this.add
+      .container(0, 0, children)
+      .setName(`held-placement:${placement.id}`)
+      .setAlpha(0.72)
+      .setDepth(5);
+  }
+
+  private getHeldPlacementGeometry(part: HeldPlacement["part"]): {
+    width: number;
+    height: number;
+    fill: number;
+    stroke: number;
+  } {
+    if (part === "ramp") {
+      const ramp = this.level.trayRamp ?? TRAY_RAMP_DEFINITION;
+      return {
+        width: ramp.width,
+        height: ramp.height,
+        fill: RAMP_FILL_COLOR,
+        stroke: RAMP_STROKE_COLOR,
+      };
+    }
+    if (part === "mattress") {
+      const mattress = this.level.trayMattress;
+      return {
+        width: mattress?.width ?? 180,
+        height: mattress?.height ?? 44,
+        fill: 0x9d6a94,
+        stroke: 0x593c58,
+      };
+    }
+    if (part === "teapot") {
+      const teapot = this.level.trayTeapot;
+      return {
+        width: teapot?.width ?? 80,
+        height: teapot?.height ?? 64,
+        fill: 0xa96d64,
+        stroke: 0x593c58,
+      };
+    }
+    return {
+      width: TRAY_BLOCK_DEFINITION.width,
+      height: TRAY_BLOCK_DEFINITION.height,
+      fill: BLOCK_FILL_COLOR,
+      stroke: BLOCK_STROKE_COLOR,
+    };
+  }
+
   spawnTrayBall(
     availableCount: number,
   ): { id: string; transform: BallTransform } | null {
diff --git a/src/main.ts b/src/main.ts
index a5711d1..7b3880c 100644
--- a/src/main.ts
+++ b/src/main.ts
@@ -61,6 +61,7 @@ const showStorybookAssetProof = new URLSearchParams(window.location.search).has(
 declare global {
   interface Window {
     __contraptionLabTest?: {
+      dispatchAction: (action: GameAction) => void;
       getPrototypeScene: () => PrototypeScene | undefined;
       getGameState: () => Readonly<GameState>;
     };
@@ -69,6 +70,7 @@ declare global {
 
 if (import.meta.env.DEV) {
   window.__contraptionLabTest = {
+    dispatchAction: (action) => handleAction(action),
     getPrototypeScene: () => scene,
     getGameState: () => state,
   };
@@ -115,6 +117,7 @@ function applyState(): void {
     state.mode === "edit" && !state.succeeded,
     state.selectedComponentId,
   );
+  scene?.setHeldPlacement(state.heldPlacement);
   scene?.setRampTransforms(state.rampTransforms);
   scene?.setBlockTransforms(state.blockTransforms);
 }
@@ -255,6 +258,7 @@ function createGame(): void {
       replaceGameState(updateBallTransform(state, ballId, transform));
       applyState();
     },
+    (transform) => handleAction({ type: "update-held-placement", transform }),
     (componentId, returnsTrayPart) =>
       handleAction({
         type: "remove-component",
diff --git a/tests/mainRoutingIntegration.test.ts b/tests/mainRoutingIntegration.test.ts
index 92f8e9e..9bb0569 100644
--- a/tests/mainRoutingIntegration.test.ts
+++ b/tests/mainRoutingIntegration.test.ts
@@ -13,6 +13,7 @@ const mainTest = vi.hoisted(() => ({
     resetLevel: vi.fn(),
     setBlockTransforms: vi.fn(),
     setEditSelection: vi.fn(),
+    setHeldPlacement: vi.fn(),
     setRampTransforms: vi.fn(),
     setSimulationRunning: vi.fn(),
     spawnTrayBall: vi.fn(),
@@ -99,6 +100,11 @@ describe("main.ts held-placement routing", () => {
       },
       trayRampCount: 4,
     });
+    expect(mainTest.scene.setHeldPlacement).toHaveBeenCalledWith({
+      part: "ramp",
+      id: "held-ramp-main-test",
+      transform: { position: { x: 0, y: 0 }, rotation: 0 },
+    });
     expect(mainTest.scene.spawnTrayBall).not.toHaveBeenCalled();
     expect(mainTest.scene.spawnTrayBird).not.toHaveBeenCalled();
     expect(mainTest.scene.spawnTrayBlock).not.toHaveBeenCalled();
