diff --git a/docs/CODEX_PROMPTS.md b/docs/CODEX_PROMPTS.md
index 69e764f..111f5c8 100644
--- a/docs/CODEX_PROMPTS.md
+++ b/docs/CODEX_PROMPTS.md
@@ -7168,3 +7168,116 @@ IMPORTANT: append THIS FULL ORIGINAL OPERATIONAL PROMPT VERBATIM to docs/CODEX_P
 Do not paraphrase, reconstruct, summarize, or omit any part of it.
 
 Then STOP.
+
+Continue Phase 2 #32 from checkpoint:
+
+65600f6bfb6dfe456ffce748a08e8c8aaaa3b778
+
+Work ONLY in:
+/home/anatolschwartz/code/contraption-lab
+
+Verify:
+- branch = phase2-32-tea-time
+- checkpoint above is an ancestor of HEAD
+- do NOT modify /home/anatolschwartz/code/contraption-lab-main
+
+Session token:
+s-20260815-1658-l32a
+
+Fix ONLY the generic Reset stale-render lifecycle bug.
+
+Observed real behavior:
+- place a Ramp from inventory;
+- Reset;
+- inventory correctly restores the Ramp;
+- the old Ramp IMAGE remains visible on the canvas;
+- the stale visual no longer behaves as a real gameplay Ramp;
+- repeated placement can therefore produce duplicate-looking Ramp visuals.
+
+This must be fixed GENERICALLY for all applicable player-placeable parts/puzzles, not Tea Time- or Ramp-specific.
+
+Required invariant after Reset:
+- runtime part records reflect reset state;
+- Matter bodies reflect reset state;
+- contact registrations reflect reset state;
+- rendered/display objects reflect reset state;
+- inventory reflects reset state;
+- no stale display object survives after its runtime/body/state object has been removed.
+
+Diagnose the existing reset path:
+resetLevel -> restoreLevelParts -> destroyParts
+and identify exactly where render-object cleanup diverges from runtime/body cleanup.
+
+Implement the smallest generic lifecycle fix.
+
+Requirements:
+- Reset destroys/removes every player-created runtime visual that should no longer exist;
+- corresponding Matter/contact/runtime entries are also removed exactly once;
+- inventory restores correctly;
+- no stale visual remains;
+- no duplicate destruction errors;
+- fixed/default level parts are restored normally;
+- existing placed-part behavior before Reset remains unchanged;
+- puzzle switching/reload may be touched ONLY if the same lifecycle helper must be corrected generically.
+
+Do NOT fix in this run:
+- Ramp visual/Matter geometry alignment;
+- placed-part movement trap;
+- Teapot collision shape;
+- Cup goal/location;
+- held Q/E rotation;
+- Escape/right-click cancel;
+- Tea Time reference-route physics;
+- unrelated layout changes.
+
+Automated verification:
+
+Focused tests:
+- place a player Ramp;
+- verify runtime/render/Matter/contact object exists;
+- Reset;
+- verify that player Ramp runtime entry is gone;
+- verify display object is gone;
+- verify Matter body/contact registration is gone;
+- verify inventory restores exactly once;
+- repeat Reset and ensure no duplicate/stale cleanup issue.
+
+Real browser regression:
+- Tea Time -> Edit -> place Ramp;
+- confirm exactly one committed Ramp;
+- click Reset;
+- confirm Ramp inventory returns to 1;
+- confirm old committed Ramp visual is gone;
+- place Ramp again;
+- confirm only one committed Ramp visual exists.
+
+Use bounded Playwright/browser timeouts and cleanly close browser/context/process handles.
+
+Run:
+- focused relevant tests
+- focused Playwright regression
+- npm run typecheck
+- npm run lint
+- git diff --check
+
+Do not commit or push Contraption Lab.
+
+Create/update:
+- s-20260815-1658-l32a-response.md
+- s-20260815-1658-l32a-diff.patch
+
+Response must clearly separate:
+- root cause
+- implementation
+- automated tests
+- browser verification
+- unresolved issues
+- manual verification requirement, if any
+
+Handoff using exactly:
+/home/anatolschwartz/code/prom-resp/push-response.sh s-20260815-1658-l32a
+
+IMPORTANT: append THIS FULL ORIGINAL OPERATIONAL PROMPT VERBATIM to docs/CODEX_PROMPTS.md.
+Do not paraphrase, reconstruct, summarize, or omit any part of it.
+
+Then STOP.
diff --git a/e2e/teaTime.e2e.ts b/e2e/teaTime.e2e.ts
index 2aa0a34..7fc12cd 100644
--- a/e2e/teaTime.e2e.ts
+++ b/e2e/teaTime.e2e.ts
@@ -121,10 +121,10 @@ test("opens Parts below the controls and starts held Teapot placement", async ({
     });
 });
 
-test("keeps an invalid held Ramp and commits a valid Ramp with normal registration", async ({
+test("cleans a committed Ramp from runtime and display state on Reset", async ({
   page,
 }) => {
-  test.setTimeout(45_000);
+  test.setTimeout(60_000);
   await openTeaTime(page);
   const parts = page.locator("#parts-palette");
   if (!(await parts.isVisible()))
@@ -399,6 +399,125 @@ test("keeps an invalid held Ramp and commits a valid Ramp with normal registrati
       runtimeRampCount: 1,
       trayRampCount: 0,
     });
+
+  await page.evaluate(() => {
+    const scene =
+      window.__contraptionLabTest?.getPrototypeScene() as unknown as {
+        ramps: Map<string, { sprite?: unknown }>;
+      };
+    const sprite = scene.ramps.get("held-ramp-1")?.sprite;
+    if (!sprite) throw new Error("Committed Ramp sprite is unavailable.");
+    (
+      window as unknown as { resetStaleRampSprite?: unknown }
+    ).resetStaleRampSprite = sprite;
+  });
+
+  await page.getByRole("button", { name: "Reset" }).click();
+  await expect
+    .poll(() =>
+      page.evaluate(() => {
+        const state = window.__contraptionLabTest?.getGameState();
+        const scene =
+          window.__contraptionLabTest?.getPrototypeScene() as unknown as {
+            children: { list: unknown[] };
+            contactObjects: Map<unknown, unknown>;
+            matter: { world: { engine: { world: { bodies: unknown[] } } } };
+            ramps: Map<string, unknown>;
+          };
+        const staleSprite = (
+          window as unknown as { resetStaleRampSprite?: unknown }
+        ).resetStaleRampSprite;
+        return {
+          contactCount: scene.contactObjects.size,
+          matterBodyCount: scene.matter.world.engine.world.bodies.length,
+          runtimeRampCount: scene.ramps.size,
+          staleSpriteInDisplayList: scene.children.list.includes(staleSprite),
+          trayRampCount: state?.trayRampCount,
+        };
+      }),
+    )
+    .toEqual({
+      contactCount: before.contactCount,
+      matterBodyCount: before.matterBodyCount,
+      runtimeRampCount: 0,
+      staleSpriteInDisplayList: false,
+      trayRampCount: 1,
+    });
+
+  await page.getByRole("button", { name: "Reset" }).click();
+  await expect
+    .poll(() =>
+      page.evaluate(() => {
+        const state = window.__contraptionLabTest?.getGameState();
+        const scene =
+          window.__contraptionLabTest?.getPrototypeScene() as unknown as {
+            children: { list: unknown[] };
+            contactObjects: Map<unknown, unknown>;
+            matter: { world: { engine: { world: { bodies: unknown[] } } } };
+            ramps: Map<string, unknown>;
+          };
+        const staleSprite = (
+          window as unknown as { resetStaleRampSprite?: unknown }
+        ).resetStaleRampSprite;
+        return {
+          contactCount: scene.contactObjects.size,
+          matterBodyCount: scene.matter.world.engine.world.bodies.length,
+          runtimeRampCount: scene.ramps.size,
+          staleSpriteInDisplayList: scene.children.list.includes(staleSprite),
+          trayRampCount: state?.trayRampCount,
+        };
+      }),
+    )
+    .toEqual({
+      contactCount: before.contactCount,
+      matterBodyCount: before.matterBodyCount,
+      runtimeRampCount: 0,
+      staleSpriteInDisplayList: false,
+      trayRampCount: 1,
+    });
+
+  await page.locator("#tray-ramp-button").click();
+  await canvas.hover({
+    position: {
+      x: (validTarget.x / 960) * canvasBox.width,
+      y: (validTarget.y / 540) * canvasBox.height,
+    },
+  });
+  await canvas.click({
+    position: {
+      x: (validTarget.x / 960) * canvasBox.width,
+      y: (validTarget.y / 540) * canvasBox.height,
+    },
+  });
+  await expect
+    .poll(() =>
+      page.evaluate(() => {
+        const state = window.__contraptionLabTest?.getGameState();
+        const scene =
+          window.__contraptionLabTest?.getPrototypeScene() as unknown as {
+            children: { list: unknown[] };
+            ramps: Map<string, { sprite?: unknown }>;
+          };
+        const staleSprite = (
+          window as unknown as { resetStaleRampSprite?: unknown }
+        ).resetStaleRampSprite;
+        const ramp = scene.ramps.get("held-ramp-1");
+        return {
+          currentSpriteInDisplayList:
+            ramp?.sprite !== undefined &&
+            scene.children.list.includes(ramp.sprite),
+          runtimeRampCount: scene.ramps.size,
+          staleSpriteInDisplayList: scene.children.list.includes(staleSprite),
+          trayRampCount: state?.trayRampCount,
+        };
+      }),
+    )
+    .toEqual({
+      currentSpriteInDisplayList: true,
+      runtimeRampCount: 1,
+      staleSpriteInDisplayList: false,
+      trayRampCount: 0,
+    });
 });
 
 test("does not send the marble to the cup without a Teapot contact", async ({
diff --git a/src/game/PrototypeScene.ts b/src/game/PrototypeScene.ts
index 5043898..7e09787 100644
--- a/src/game/PrototypeScene.ts
+++ b/src/game/PrototypeScene.ts
@@ -2033,12 +2033,8 @@ export class PrototypeScene extends Phaser.Scene {
   }
 
   private destroyParts(): void {
-    for (const ramp of this.ramps.values()) {
-      this.unregisterContactObject(ramp.shape);
-      ramp.shape.destroy();
-      ramp.highlight.destroy();
-      ramp.shadow.destroy();
-      ramp.selectionText.destroy();
+    for (const ramp of [...this.ramps.values()]) {
+      this.destroyRamp(ramp.definition.id);
     }
     for (const block of this.blocks.values()) {
       this.unregisterContactObject(block.shape);
@@ -2061,7 +2057,6 @@ export class PrototypeScene extends Phaser.Scene {
       teapot.selectionText.destroy();
       teapot.sprite?.destroy();
     }
-    this.ramps.clear();
     this.blocks.clear();
     this.mattresses.clear();
     this.teapots.clear();
@@ -2195,6 +2190,10 @@ export class PrototypeScene extends Phaser.Scene {
   }
 
   private destroyRampFromContact(componentId: string): void {
+    this.destroyRamp(componentId);
+  }
+
+  private destroyRamp(componentId: string): void {
     const ramp = this.ramps.get(componentId);
     if (!ramp) return;
     this.unregisterContactObject(ramp.shape);
